<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>うどんの種類</title>
</head>
<body>
  <h2>うどんの種類一覧</h2>
  <table>
    <tr><th>ID</th><th>うどんの種類</th></tr> <% let number = 0 %>
    <% for( let row of data ) { %>
      
      <% if (row) { %> <tr>
          <td><%= row.id %></td>
          <td><a href="/udon/<%= number %>"><%= row.name %></a></td>
        </tr>
      <% } %>
      
      <% number += 1 %> <% } %>
    
  </table>
  <br>
  <a href="/udon/create">追加</a>
  <a href="/">topに戻る</a>

</body>
</html>